# URL base da API REST do Django (sem barra final).
# Em docker-compose o serviço web já injeta http://localhost:8000.
VITE_API_URL="http://localhost:8000"

# Cloudflare Turnstile (captcha do registro) — chave pública do site.
VITE_TURNSTILE_SITE_KEY="1x00000000000000000000AA"
